name: Apex BTC Bot (Legacy - Disabled)

# Disabled — replaced by apex-btc-bot.yml running run-bot-clean.mjs
on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled"
